{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pypi_push",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "git push && rm -rf dist build *.egg-info && python3.12 -m build && twine upload dist/*"
            ],
            "problemMatcher": []
        }
    ]
}