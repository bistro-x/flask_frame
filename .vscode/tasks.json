{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pypi_push",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "git push  && source ~/.bashrc && rm -rf dist && python3.12 setup.py sdist bdist_wheel && twine upload dist/*"
            ],
            "problemMatcher": [],
        }
    ]
}